name: Auto Publish AI Posts

on:
  schedule:
    - cron: "0 6 * * *"   # every day
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install node-fetch@2

      - name: Generate AI Posts
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node generate_post.js

      - name: Commit and push
        run: |
          git config user.name "rakshan0027"
          git config user.email "198556807+rakshan0027@users.noreply.github.com"
          git add posts.json
          git commit -m "🤖 Auto-published Tech + Finance post"
          git push
