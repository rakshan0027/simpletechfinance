name: Auto Publish AI Posts

on:
  schedule:
    - cron: "0 6 * * *"   # once per day
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - run: npm install node-fetch

      - name: Generate AI Post
        run: node generate_post.js
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Commit and push
        run: |
          git config user.name "rakshan0027"
          git config user.email "198556807+rakshan0027@users.noreply.github.com"
          git add posts.json
          git commit -m "🤖 Auto-published AI post" || echo "No changes"
          git push
